unity版本2018.4.8
QFramework 0.1.11
GameDesigner 2019.9.8

QFramework 0.1.11框架修改UIKit
UIManager中的方法替换为
public void CloseAllUI()
{
         foreach (var layer in mAllUI)
         {
                if((layer.Value as UIPanel))
                {
                    layer.Value.Close();
                }
         Destroy(layer.Value.Transform.gameObject);
         }
         mAllUI.Clear();
}

unity自身做服务器,发布出客户端测试

已完成
	Udp服务器
	客户端连接注册(未加入数据库)
	大厅模式
	聊天系统(战斗场景内部聊天系统不与外部干扰)
	创建房间\加入房间\退出房间
	场景跳转过度功能
接下来的目标
	服务器整改
		---大厅用http服务器
		---战斗场景使用Udp服务器+帧同步  
	人物生成 + 人物移动 + 人物动画   控制


服务器 场景类有所改动 添加生成玩家的位置信息 ,没有传入 生成系统,等待验证
	